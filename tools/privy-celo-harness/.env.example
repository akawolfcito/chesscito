# Copy to .env.local and set your Privy DEVELOPMENT App ID.
# The App ID is a public, client-side identifier — safe for the browser bundle.
VITE_PRIVY_APP_ID=

# DO NOT add PRIVY_APP_SECRET here. This harness never uses the App Secret.
# The App Secret is a server-only credential and has no place in a client bundle.
