/**
 * ES message bundle for next-intl.
 *
 * Stage 1 stub: empty record. The bundle gets populated in Stage 4
 * of the migration, after Stage 3 wires components onto
 * `useTranslations`. Until then, `/es/*` URLs render but show
 * EN copy from `lib/content/editorial.ts`.
 *
 * See: docs/superpowers/specs/2026-05-23-i18n-es-en-design.md
 */
const messages = {} as const;

export default messages;
