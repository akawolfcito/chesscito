/**
 * EN message bundle for next-intl.
 *
 * Stage 1 stub: empty record so the bundle loader has something to
 * resolve. The active source of truth for EN copy is still
 * `lib/content/editorial.ts`. Stage 2 of the i18n migration moves
 * editorial values into this file and adds a back-compat shim.
 *
 * See: docs/superpowers/specs/2026-05-23-i18n-es-en-design.md
 */
const messages = {} as const;

export default messages;
